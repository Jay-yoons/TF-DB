spring.application.name=store-service

# H2 Database 설정 (테스트용)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# H2 Console 활성화 (브라우저에서 http://localhost:8080/h2-console 접속 가능)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA 설정
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Thymeleaf 설정
spring.thymeleaf.cache=false

# Google Maps API Key
google.maps.api.key=AIzaSyB6yKm_vZw2nCWECG9Ju_Ytg2i19W3L96s

# 프로필 활성화는 외부 환경변수/실행 인자로 제어 권장
# spring.profiles.active=dev

# 활성 프로필(dev에서만 DataInitializer 동작)